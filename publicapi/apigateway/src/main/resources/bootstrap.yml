spring:
  application:
    name: publicapi-gateway
  # nacos
  cloud:
    nacos:
      # 注册中心的配置
      discovery:
        server-addr: 127.0.0.1:8848
        # namespace: mkt_dev
        # group: DEFAULT_GROUP
        register-enabled: false

